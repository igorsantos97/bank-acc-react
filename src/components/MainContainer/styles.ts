import styled from 'styled-components';

export const Container = styled.div`
  max-width: 1120px;
  margin: 0 auto;
  width: 100%;
  height: inherit;
  padding: 0 16px;
`;

export default Container;
